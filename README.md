# The Terry Header
> You run them over, that's what you do.

`terry.h` defines short-form fixed size numeric types
and low level (bitwise) operations which inherently require fix size types.

## Interface
```c
// XXX list the types

T rol(T, n);
T ror(T, n);
```
